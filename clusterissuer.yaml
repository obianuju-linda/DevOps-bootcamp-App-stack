apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: bootcampt-issuer
spec:
  acme:
    email: obianujuowoh396@gmail.com
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: bootcampt-issuer-account-key
    solvers:
    - dns01:
        cloudflare:
          email: obianujuowoh396@gmail.com
          apiTokenSecretRef:
            name: cloudflare-api-token-secret
            key: api-token
